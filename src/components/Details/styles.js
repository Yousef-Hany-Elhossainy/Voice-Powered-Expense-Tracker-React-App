import { makeStyles, useStyles } from "@material-ui/core";

export default makeStyles(() => ({
  income: {
    borderBottom: "10px solid rgba(30,255,150,0.7)",
  },
  expense: {
    borderBottom: "10px solid rgba(30,150,255,0.7)",
  },
}));
